describe("setCookie", () => {
  it("throws an error when given an invalid value", () => {
    // test implementation
  });

  it("returns a cookie string when given valid inputs", () => {
    // test implementation
  });

  it("defaults the expiry date to 7 days when no duration is provided", () => {
    // test implementation
  });
});
